# Sync Status

Brief status report. Keep it under 10 lines total.

1. Read `docs/FEATURES.md` if it exists (skip if not)
2. Check git state: current branch, unpushed commits, dirty files
3. Report:
   - Last 3 commits (titles only)
   - Unpushed commit count
   - Dirty files (count + list if under 10)
   - Current module being worked on (from latest handoff if one exists, otherwise "no handoff yet")
   - Suggested next action

Format: plain text, no markdown headers, no explanations. Just the facts.
