# Orient Fresh Session

<!-- TEMPLATE: Customize the paths below for your project. -->

This is a fresh session. Orient yourself without burning context:

1. Read `CLAUDE.md` for project context (auto-loaded)
2. Read `MEMORY.md` index and the latest dated handoff
3. Run `git log --oneline -10` to see recent work
4. Run `git status` to see uncommitted changes
5. Check `docs/FEATURES.md` or equivalent module status table

Then report in 5 lines or less:
- What was last worked on
- What's next (from latest handoff)
- Any uncommitted changes or blockers
- Current branch

Do NOT start implementing anything. Wait for instructions.
